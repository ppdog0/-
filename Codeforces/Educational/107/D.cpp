#include <iostream>

using namespace std;
using ll = long long;

void solve() {
    int n, k;
    cin >> n >> k;
    for () {}
}

int main() {
    cin.sync_with_stdio(false);
    cin.tie(0);
    solve();
}