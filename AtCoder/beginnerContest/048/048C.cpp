#include<iostream>
#include<vector>

using namespace std;

int N;
long long x;

int main() {
    cin >> N >> x;
    vector<long long> nums(N);
    for (int i = 0; i < N; i++)
        cin >> nums[i];
}